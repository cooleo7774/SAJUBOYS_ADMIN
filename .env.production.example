# Cloudflare Pages 권장값 (실제 API 주소로 교체)
VITE_DEFAULT_API_BASE_URL=https://api.saju-boys.com

# 자체 서버에서 같은 도메인 /api 프록시를 쓰는 경우:
# VITE_DEFAULT_API_BASE_URL=/api

# 운영 모드: 상단 입력칸 숨김 + 자동 Connect 전용 UI
VITE_ADMIN_OPERATION_MODE=true
